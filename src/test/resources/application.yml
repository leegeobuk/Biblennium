spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/biblennium?characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
    username: root
    password: freestyle2011

  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
